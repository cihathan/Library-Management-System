@model MvcLibrary.Models.Entity.Category
@{
    ViewBag.Title = "GetCategory";
    Layout = "~/Views/Shared/Layout.cshtml";
}

@using (Html.BeginForm("UpdateCategory", "Category", FormMethod.Post))
{ 
    <div style="margin-bottom:15px"></div>
    <div class="form-group">
        @Html.LabelFor(category=> category.CategoryID)
        @Html.TextBoxFor(category => category.CategoryID, new {@class="form-control"})
        @*@Html.LabelFor(category => category.Name)*@
    <label>KATEGORİ ADI</label>     
        @Html.TextBoxFor(category => category.Name, new {@class="form-cont"})
    </div>
    <div>
        <button class="btn btn-warning">Güncelle</button>
    </div>
    
}
